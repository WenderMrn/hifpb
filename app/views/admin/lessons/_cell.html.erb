<%=
if day.nil?
  "-"
else
  if day.schoolroom.nil? and day.laboratory.nil?
    @display = day.laboratory.name +" ~ "+ day.laboratory.name
  else
    @display = day.schoolroom.nil? ? day.laboratory.name : day.schoolroom.name
  end
  link_to(@display, edit_admin_classroom_lesson_path(day.classroom, day))
end
%>